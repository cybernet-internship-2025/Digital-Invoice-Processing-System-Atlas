<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="az.cybernet.usermanagement.mapper.GovernmentTaxOrganizationMapper">

    <select id="findById" resultType="az.cybernet.usermanagement.entity.GovernmentTaxOrganization">
        SELECT * FROM users.government_tax_organizations WHERE id = #{id}
    </select>

</mapper>
