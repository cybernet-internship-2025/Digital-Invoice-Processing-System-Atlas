databaseChangeLog:
  - changeSet:
      id: 202508261040
      author: GoychakAhmadova
      changes:
        - dropColumn:
            tableName: user_details
            schemaName: users
            columnName: tax_id

        - modifyDataType:
            tableName: users
            schemaName: users
            columnName: tax_id
            newDataType: VARCHAR(10)

        - addColumn:
            tableName: users
            schemaName: users
            columns:
              - column:
                  name: password
                  type: VARCHAR(255)
                  constraints:
                    nullable: false

        - addColumn:
            tableName: user_details
            schemaName: users
            columns:
              - column:
                  name: date_of_birth
                  type: date
                  constraints:
                    nullable: false

        - addColumn:
            tableName: user_details
            schemaName: users
            columns:
              - column:
                  name: organization_id
                  type: varchar2(40)
                  constraints:
                    nullable: false

        - addForeignKeyConstraint:
            baseTableSchemaName: users
            baseTableName: user_details
            baseColumnNames: organization_id
            referencedTableSchemaName: users
            referencedTableName: government_tax_organizations
            referencedColumnNames: id
            constraintName: fk_userdetails_organization