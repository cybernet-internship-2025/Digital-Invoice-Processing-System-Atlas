<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="az.cybernet.invoice.mapper.InvoiceViewMapper">
    <select id="getSentInvoicesByTaxId" resultType="Invoice" parameterType="string">
        SELECT *
        FROM invoice.invoice
        WHERE invoice.sender_id IN (
            SELECT id
            FROM users.users
            WHERE tax_id = #{taxId}
        ) AND invoice.status != 'DRAFT'
    </select>

    <select id="getReceivedInvoicesByTaxId" resultType="Invoice" parameterType="string">
        SELECT *
        FROM invoice
        WHERE invoice.customer_id IN (
            SELECT id
            FROM users.users
            WHERE tax_id = #{taxId}
        ) AND invoice.status != 'DRAFT'
    </select>

    <select id="getAllDraftsByTaxId" resultType="Invoice" parameterType="string">
        SELECT *
        FROM invoice.invoice
        WHERE invoice.sender_id IN (
            SELECT id
            FROM users.users
            WHERE tax_id = #{taxId}
        ) AND invoice.status = 'DRAFT'
    </select>

    <select id="getAllInvoicesByTaxId" resultType="Invoice" parameterType="string">
        SELECT *
        FROM invoice
        WHERE invoice.status != 'DRAFT' AND
              (invoice.sender_id IN (
                  SELECT id
                  FROM users.users
                  WHERE tax_id = #{taxId}
              ) OR invoice.customer_id IN (
                  SELECT id
                  FROM users.users
                  WHERE tax_id = #{taxId}
              ))
    </select>
</mapper>