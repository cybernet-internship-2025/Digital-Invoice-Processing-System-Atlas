server:
  servlet:
    context-path: /api/invoice

spring:
  datasource:
    url: jdbc:postgresql://postgres-db:5432/${INVOICE_DB_NAME:invoice}
    username: ${INVOICE_DB_USER:postgres}
    password: ${INVOICE_DB_PASS:Goychak2003}

  liquibase:
    change-log: classpath:liquibase/changelog-master.xml
    enabled: false

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

logging:
  level:
    org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration: DEBUG
    liquibase: DEBUG
